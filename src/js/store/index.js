/**
 * The external dependencies.
 */
import Vue from 'vue';
import Vuex from 'vuex';

/**
 * Install the plugin.
 */
Vue.use(Vuex);

/**
 * Initialize the store.
 */
export default new Vuex.Store({});
